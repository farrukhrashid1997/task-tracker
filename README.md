### Things to add to make improve the code:
- Tickets and Accounts module add serializers
    - we can automatically validate incoming data 
    - less repetitive code

- In the dashboard, create an api which returns the tickets over time
- Create an api which handles the dropdowns within the app
- Unify the buttons in the app
- the sidebar hamburger menu doesnt come out, check it